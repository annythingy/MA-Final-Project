\ProvidesPackage{preamble}
\usepackage[a4paper, margin=1in, top=2.4cm, headheight=24mm, marginparwidth=2cm]{geometry}
\usepackage{newtxtext,newtxmath} % Times New Roman
\usepackage[table,dvipsnames]{xcolor}
\usepackage{fancyhdr}\pagestyle{fancy}
\usepackage{xurl}
\usepackage{hyperref}
\hypersetup{colorlinks=true, linkcolor={Sepia}, urlcolor={MidnightBlue}, citecolor={Mahogany}}
\usepackage{enumitem} 
\usepackage{xparse}    
\usepackage{graphicx}
\usepackage{multicol}
\usepackage{marginnote}
\usepackage{fontawesome5}
\setlength{\arrayrulewidth}{0.16mm}\arrayrulecolor{gray}
\usepackage{setspace}
\usepackage{csquotes}
\usepackage{titlesec}

\usepackage[backend=biber, style=apa, sorting=nyt]{biblatex}
% \usepackage[style=numeric]{biblatex} % TEMP/HACK: to count refs
  \ExecuteBibliographyOptions{doi=true,url=false}
  \DeclareFieldFormat{doi}{\url{https://doi.org/#1}} %\newline if needed
  \DeclareFieldFormat{addendum}{\newline Available at: \url{#1}} %\newline if needed
  \AtBeginBibliography{\raggedright}
  \addbibresource{bib/reads.bib}
  \addbibresource{bib/plays.bib}
  \addbibresource{bib/tools.bib}
  \DeclareCiteCommand{\citeauthor}
    {\usebibmacro{prenote}}
    {\printnames[given-family]{author}} % for full name
    {\multicitedelim}
    {\usebibmacro{postnote}}
  \NewDocumentCommand{\citebook}{m}{\citetitle{#1}, a book by \citeauthor{#1} \parencite*{#1}}
  \NewDocumentCommand{\citework}{m}{\textit{\citetitle{#1}}, an article by \citeauthor{#1} \parencite*{#1}}
  \NewDocumentCommand{\citeconf}{m}{\textit{\citetitle{#1}}, a conference paper by \citeauthor{#1} \parencite*{#1}}
  \defbibfilter{readings}{type=article or type=book or type=collection or type=incollection or type=inproceedings or type=online or type=unpublished}
  \defbibfilter{assets}{type=www or type=image or type=misc}

\titleformat{\chapter}[hang]{\bfseries\LARGE\scshape\centering\vspace{-8mm}}{Chapter \thechapter}{.8ex}{}
\titleformat{\section}[block]{\bfseries\Large\vspace{-8pt}}{\thesection}{8pt}{\vspace{-8pt}}

\renewcommand\contentsname{Summary\vspace{-8mm}}
\renewcommand\listfigurename{Illustrations\vspace{-8mm}}

\newenvironment{my-abstract}
  {\begin{center}\begin{singlespace}}
  {\end{singlespace}\end{center}}

\newcommand{\keywords}[1]{
  \reversemarginpar\marginnote{\footnotesize\textsc{Keywords:}\textit{#1}}[4cm]
  \normalmarginpar} % aesthetics, authenticity, experience, immersion, materiality, metagaming, nostalgia, paraphernalia, paratext, preservation, retrospective, transmedia

\newcommand{\wordCount}[2]{\vspace{-8mm}\hfill\textit{\small(#1/#2 words)}}

\definecolor{chap0}{RGB}{100, 200, 200}
\definecolor{chap1}{RGB}{100, 150, 200}
\definecolor{chap2}{RGB}{100, 100, 200}
\definecolor{chap3}{RGB}{150, 100, 200}
\definecolor{chap4}{RGB}{200, 100, 200}
\definecolor{chapZ}{RGB}{250, 100, 200}

\usepackage{tikz}
\usetikzlibrary{shapes.geometric, arrows, arrows.meta}
\usetikzlibrary{positioning, calc, decorations.text}
    \tikzstyle{startstop} = [rectangle, rounded corners, minimum width=3cm, minimum height=1cm, text centered, draw=black, fill=yellow!30]
    \tikzstyle{io} = [trapezium,  trapezium stretches=true, trapezium left angle=70,  trapezium right angle=110,  minimum width=3cm,  minimum height=1cm, text centered,  draw=black, fill=blue!30]
    \tikzstyle{process} = [rectangle, minimum width=3cm, minimum height=1cm, text centered, text width=3cm, draw=black, fill=orange!30]
    \tikzstyle{decision} = [diamond, minimum width=3cm, minimum height=1cm, text centered, draw=black, fill=green!30]
    \tikzstyle{arrow} = [thick,->,>=stealth]
    \tikzstyle{pTile} = [rectangle, draw, rounded corners, text centered]

\newcommand{\tikzchapters}{
  \begin{center}\begin{tikzpicture}
    % 5% = 0.6, 17.5% = 2.1, 20% = 2.4, 22.5% = 2.7, 30% = 3.6 w/ 0.2 buffer
    \draw[chap0, line width=2mm] (0,0)     -- (0.6, 0); 
    \draw[chap1, line width=2mm] (0.8, 0) -- (4.4, 0);
    \draw[chap2, line width=2mm] (4.6, 0) -- (7.3, 0);
    \draw[chap3, line width=2mm] (7.5, 0) -- (9.6,0);
    \draw[chap4, line width=2mm] (9.8,0) -- (12.2,0);
    \draw[chapZ, line width=2mm] (12.4,0) -- (13, 0);
  \end{tikzpicture}\end{center}
}

\newcommand{\tikzperi}{
  \begin{center}\begin{tikzpicture}[node distance=32mm]
    % Central node
    \node (play) [pTile] {\textsc{Play}};
    % Radius of the circle
    \def\radius{32mm}
    % Peripheral nodes in circular layout
    \node (paratxt) [pTile, above of=play] {\textsc{Paratextuality}};
    \node (physics) [pTile, below of=play, xshift=-36mm, yshift=12mm] {\textsc{Physicality}};
    \node (popular) [pTile, below of=play, xshift= 36mm, yshift=12mm] {\textsc{Popularity}};
    % Connections to central node
    \draw [very thin, >=triangle 45, <->] (play) -- (paratxt);
    \draw [very thin, >=triangle 45, <->] (play) -- (physics);
    \draw [thin, >=triangle 45, <->] (play) -- (popular);
    % Dotted lines and labels
    \path [dotted, very thick] (paratxt) edge[bend right] node[midway, left]  {\small\textit{paraphernalia}} (physics);
    %\draw [dotted,thick](paratxt)--(popular)node[midway,right,xshift=4mm,yshift=-4mm]{\small\textit{or proliferation?}};
    \path [dotted, very thick] (paratxt) edge[bend left] node[midway, right]  {\small\textit{personalization}} (popular);
    \path [dotted, very thick] (physics) edge[bend right] node[midway, below] {\small\textit{preservation}} (popular);
    % Circular dashed periphery
    \draw [violet, thick, dashed] (play) circle (16mm);
    %\node (play) [left, gray, xshift=8mm, yshift=-2cm] {\footnotesize\textit{periphery}};
    \begin{scope}[transform canvas={xscale=-1,yscale=-1}]
        \path[decorate,decoration={text along path, text={perimeter},text align=center}] (1.78,0) arc[start angle=0,end angle=-300,radius=1.78];
        \path[decorate,decoration={text along path, text={perimeter},text align=center}] (1.78,0) arc[start angle=0, end angle=-70,radius=1.78];
        \path[decorate,decoration={text along path, text={perimeter},text align=center}] (1.88,0) arc[start angle=0, end angle=180,radius=1.88];
    \end{scope}
  \end{tikzpicture}\end{center}
}

\setlist[description]{style=nextline, leftmargin=2em, labelsep=1em}
\newcommand{\srcBullet}[1]{\\\textcolor{#1}{\hspace{-8pt}\faAngleRight}}
\NewDocumentCommand{\chapterplan}{m m m m m m}{
  \item[\textcolor{#1}{\textbf{\large#2}}\hfill\textit{\footnotesize$\sim$#3}]
    \phantom{.}\vspace{-4mm}\newline#4\vspace{-2mm}
  \begin{itemize}[leftmargin=0em, labelindent=0pt, labelwidth=1.4em, labelsep=0.4em, noitemsep]
    \item[\textcolor{#1}{\faCog}]         \textsc{\small Methods:} {\small #5}
    \item[\textcolor{#1}{\faLightbulb\>}] \textsc{\small Sources:} {\small #6}
  \end{itemize}
  \vspace{-8mm}\tikzchapters
}

\usepackage[colorinlistoftodos]{todonotes}
  \setuptodonotes{fancyline, size=\footnotesize}
   \todostyle{ref}{noinline, color = yellow!32}
   \todostyle{add}{  inline, color = orange!64}
   \todostyle{rtd}{  inline, color = purple!32}
   \todostyle{dev}{  inline, color = purple!64}
   \todostyle{app}{  inline, color = blue!32}
   \todostyle{gpt}{  inline, color = blue!16}
   \todostyle{end}{  inline, color = gray!48}